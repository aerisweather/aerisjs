/**
 * @fileoverview Provides configuration settings for geocode service
 * as defined in require.config's config['aeris/geocode/config'] object.
 */
define(['module'], function(module) {
  return module.config();
});
