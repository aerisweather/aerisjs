{{#ifTopLevelNs name}}
  <h2 id="{{name}}">{{nsName this}}</h2>
{{else}}
  <span id="{{name}}"></span>
{{/ifTopLevelNs}}

{{#each classes}}
  {{>class this}}
{{/each}}

{{#each namespaces}}
  {{! Recursively render child namespaces }}
  {{>classdocs this}}
{{/each}}